torch>=2.1
transformers>=4.49        # SeamlessM4Tv2 needs a recent build
datasets>=2.18
torchaudio                # resampling to 16 kHz
sentencepiece             # SeamlessM4T tokenizer

# IndicConformer (custom_code via trust_remote_code) pulls ONNX:
onnx==1.20.1
onnxruntime-gpu==1.20.1   # onnxruntime==1.20.1 if no GPU

# OWSM-CTC runs through ESPnet (NOT transformers). Heavy install (~few min);
# flash-attn is NOT needed for inference. Best installed on its own.
espnet
espnet_model_zoo
librosa
soundfile

numpy
# WER/CER are built into asr_eval.py (no jiwer needed) -> offline scoring works.
